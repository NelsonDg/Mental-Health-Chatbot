# Get a free key from Google AI Studio: https://aistudio.google.com
GEMINI_API_KEY=your-gemini-api-key-here

# Leave blank. app.py generates one automatically on first run and
# persists it back into your local .env so sessions survive restarts.
FLASK_SECRET_KEY=
